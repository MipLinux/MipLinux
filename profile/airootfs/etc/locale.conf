# MipLinux · 全局默认语言
#
# 与 locale.gen 里的 zh_CN.UTF-8 是成对的：只改这里、不改 locale.gen，
# 会得到 "setlocale: cannot change locale" 并把消息退回英文。
#
# 已知副作用：tty（内核 VT）下 systemd/agetty 的提示会变中文，而 VT 字体
# 塞不下汉字（见 vconsole.conf），所以开机、登录提示可能出现方块。
# 图形会话不受影响。
LANG=zh_CN.UTF-8
